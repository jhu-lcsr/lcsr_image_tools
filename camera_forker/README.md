
Camera Forker
=============

This package provides a ROS camera topic "forker" which can take a single image
stream, apply arbitrary affine transformations to it, and then re-broadcast it
as one or more sub-streams.
